# Тестирование бота

> Связано: [ТЗ](../../ТЗ.md) | [Архитектура](../../Архитектура.md) | [Бэклог](../../Бэклог.md)

---


## Быстрый тест Garmin-интеграции

### 1. Тест без запуска бота

```bash
cd bot_trainer
source venv/bin/activate
python test_garmin.py
```

**Ожидаемый результат:**
- Если есть тренировки за сегодня: `✅ Сохранено N тренировок`
- Если нет тренировок: `ℹ️ Тренировок за сегодня нет`

### 2. Проверка зон пульса

Посмотреть сохранённые данные:

```bash
sqlite3 data/training_bot.db
```

```sql
-- Показать последние тренировки с зонами
SELECT date, distance_km, avg_hr, hr_zones
FROM trainings
ORDER BY date DESC
LIMIT 5;
```

**Ожидаемый формат `hr_zones`:**
```json
{"z1": 0, "z2": 2700, "z3": 900, "z4": 300, "z5": 0}
```

Выйти: `.exit`

### 3. Загрузка плана тренировок

```bash
cd bot_trainer
source venv/bin/activate
python scripts/load_training_plan.py
```

**Ожидаемый результат:**
- `✅ Загружено 49 тренировок`
- Показывает примеры загруженных тренировок

### 4. Тест отображения плана

```bash
python scripts/test_plan_display.py
```

**Ожидаемое:**
- Выводит план на неделю в отформатированном виде

### 5. Тест через Telegram

```bash
python main.py
```

В Telegram:
1. `/start` — проверка приветствия
2. `/sync` — синхронизация с Garmin
3. `/plan` — показ плана на текущую неделю
4. `/help` — список команд

**Ожидаемое:**
- `/sync`: "📥 Синхронизирую тренировки с Garmin..." → "✅ Сохранено N тренировок"
- `/plan`: План на неделю с датами, типами тренировок, длительностью, зонами

### 6. Тест адаптации плана

```bash
python scripts/test_adaptation.py
```

**Ожидаемое:**
- Симуляция пропуска тренировки 29.12.2025
- Адаптация: +40мин к следующей аналогичной тренировке (19.01)
- Лог изменений в плане

### 7. Тест статистики

```bash
python scripts/test_stats.py
```

**Ожидаемое:**
- Создаётся 4 тестовые тренировки за последнюю неделю
- Статистика за неделю: 50км, 5ч 5мин, средний пульс 160
- Зоны пульса: Z1 10%, Z2 69%, Z3 17%, Z4 3%
- Дни до забегов

## Проверка что работает

✅ БД создаётся автоматически
✅ Индексы применяются
✅ Context manager для сессий
✅ Garmin авторизация
✅ Парсинг беговых тренировок
✅ Парсинг зон пульса (3 формата)
✅ Сохранение в БД
✅ Проверка дубликатов
✅ Парсинг плана тренировок из markdown
✅ Загрузка плана в БД
✅ Адаптация плана при пропуске (перераспределение объёма)
✅ Адаптация плана при перевыполнении
✅ Адаптация по самочувствию
✅ Scheduler (00:00 - анализ, 01:00 - план)
✅ Статистика (/stats week, /stats month)
✅ Расчёт зон пульса и прогресса к целям
✅ Telegram команды: /start, /help, /sync, /plan, /stats

## Что ещё не работает (в разработке)

🔄 Вечерний опрос самочувствия
🔄 Google Calendar интеграция
🔄 AI-консультант (советы после тренировок)
🔄 AI-консультант
